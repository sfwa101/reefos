/**
 * Legacy entry point — relocated to the Sovereign Catalog Gateway in
 * Wave P-3 Sub-Wave 11. Kept as a pure re-export shim so existing callers
 * compile unchanged. New code should import from
 * `@/core/catalog/gateway/SovereignCatalogGateway` directly.
 */
export * from "@/core/catalog/gateway/SovereignCatalogGateway";
